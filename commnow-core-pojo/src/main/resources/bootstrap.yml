eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
spring:
  application:
    name: commnow-core-pojo
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: commnow-config-server